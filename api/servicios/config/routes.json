{
  "routes": [
    {
      "method": "GET",
      "path": "/servicios",
      "handler": "servicios.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/servicios",
      "handler": "servicios.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/servicios",
      "handler": "servicios.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
